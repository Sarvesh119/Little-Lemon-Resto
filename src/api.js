// src/api.js

export const fetchAPI = (date) => {
  // Simulate available booking times based on date
  return ['17:00', '18:00', '19:00', '20:00'];
};

export const submitAPI = (formData) => {
  // Simulate successful form submission
  return true;
};
